version: '2.1'
services:

networks:
cybererp-CUSTOMER-net:
    driver: bridge
    driver_opts:
      com.docker.network.bridge.name: br-CUSTOMER
    enable_ipv6: false
    ipam:
      driver: default
     
