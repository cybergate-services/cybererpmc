version: '2.1'
services:

  portainer:
    image: hub.cybergate.lk/cybererp/portainer:1.0.0
    container_name: portainer
    hostname: portainer
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./conf/portainer:/data
    restart: always
    networks:
